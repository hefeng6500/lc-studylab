# 第 3 阶段快速开始指南

## 🚀 5 分钟快速体验

### 前置条件

1. ✅ 已完成第 1、2 阶段的环境配置
2. ✅ 已配置 `.env` 文件
3. ✅ 虚拟环境已激活

### 步骤 1: 安装依赖（如果还没有）

```bash
cd backend
source venv/bin/activate
pip install langgraph
```

### 步骤 2: 运行测试脚本

```bash
./scripts/test_workflow.sh
```

这将自动运行 3 个测试：
- ✅ 完整工作流测试
- ✅ 检查点恢复测试
- ✅ 重试机制测试

### 步骤 3: 使用 API（可选）

**启动服务器：**
```bash
./start_server.sh
```

**测试 API：**
```bash
# 1. 启动工作流
curl -X POST http://localhost:8000/workflow/start \
  -H "Content-Type: application/json" \
  -d '{"user_question": "我想学习 Python 的基础知识"}'

# 保存返回的 thread_id，例如: study_abc123

# 2. 提交答案
curl -X POST http://localhost:8000/workflow/submit-answers \
  -H "Content-Type: application/json" \
  -d '{
    "thread_id": "study_abc123",
    "answers": {
      "q1": "B",
      "q2": "变量",
      "q3": "Python 是一种高级编程语言"
    }
  }'
```

## 📖 工作流说明

### 工作流程

```
用户提问 
  ↓
📋 生成学习计划（主题、目标、知识点）
  ↓
📄 检索相关文档
  ↓
📝 生成练习题（选择题、填空题、简答题）
  ↓
⏸️  暂停，等待用户答题
  ↓
📊 自动评分
  ↓
💬 生成反馈
  ↓
🔄 如果得分 < 60，重新出题（最多 3 次）
```

### 示例输出

**学习计划：**
```
主题: Python 基础知识
难度: beginner
预计时间: 60 分钟

学习目标:
1. 理解变量和数据类型
2. 掌握基本的控制流
3. 能够编写简单的 Python 程序

关键知识点:
- 变量命名规则
- 数据类型（int, str, list, dict）
- if/else 语句
- for/while 循环
- 函数定义
```

**练习题：**
```
第 1 题 (10 分) - 选择题
Python 中哪个是正确的变量命名？
A. 1variable
B. variable_1
C. variable-1
D. variable 1

第 2 题 (15 分) - 填空题
在 Python 中，用于定义函数的关键字是 _____

第 3 题 (25 分) - 简答题
请简要说明 Python 中 list 和 tuple 的区别。
```

**评分结果：**
```
总分: 85 分
答对题数: 4/5

详细评分:
✅ 第 1 题: 10/10 分
✅ 第 2 题: 15/15 分
❌ 第 3 题: 10/25 分
   您的答案: list 可以修改
   正确答案: list 是可变的，tuple 是不可变的...
   反馈: 答案部分正确，但不够完整...
```

## 🔧 常见问题

### Q1: 测试脚本运行失败？

**检查清单：**
- [ ] 虚拟环境已激活
- [ ] `.env` 文件已配置
- [ ] OpenAI API Key 有效
- [ ] 已安装 langgraph

### Q2: 工作流卡住不动？

**解决方案：**
```python
# 查看当前状态
from workflows.study_flow_graph import get_workflow_state

state = get_workflow_state("your_thread_id")
print(state.get("current_step"))
print(state.get("error"))
```

### Q3: 检查点数据库锁定？

**解决方案：**
```bash
# 关闭所有测试进程
pkill -f test_workflow

# 删除数据库重新开始
rm data/checkpoints/study_flow.db
```

## 📚 下一步

- 📖 阅读完整文档：`docs/stage_03/README.md`
- 🔧 查看技术实现：`docs/stage_03/IMPLEMENTATION.md`
- 🎉 查看完成总结：`docs/stage_03/STAGE3_COMPLETION.md`

## 💡 提示

- 工作流会在生成练习题后自动暂停，等待用户提交答案
- 每个 thread_id 对应一个独立的工作流实例
- 检查点会自动保存，支持随时恢复
- 得分低于 60 分会自动重新出题，最多 3 次

## 🎯 快速测试命令

```bash
# 完整测试套件
./scripts/test_workflow.sh

# 只测试 API
./start_server.sh
# 然后访问 http://localhost:8000/docs

# Python 交互式测试
python
>>> from workflows.study_flow_graph import start_study_flow
>>> result = start_study_flow("学习 Python", "test_123")
>>> print(result)
```

祝您使用愉快！🎉

